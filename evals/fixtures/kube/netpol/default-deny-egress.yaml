apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redrob-den-default-deny-egress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: redrob-ee
      app.kubernetes.io/instance: redrob-ee
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: In
        values:
          - den-api
          - den-web
  policyTypes:
    - Egress
  egress: []
