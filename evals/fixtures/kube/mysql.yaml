apiVersion: v1
kind: Secret
metadata:
  name: redrob-mysql
  labels:
    app.kubernetes.io/name: redrob-mysql
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: password
  MYSQL_DATABASE: redrob_den
  MYSQL_USER: redrob
  MYSQL_PASSWORD: redrob
---
apiVersion: v1
kind: Service
metadata:
  name: redrob-mysql
  labels:
    app.kubernetes.io/name: redrob-mysql
spec:
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  selector:
    app.kubernetes.io/name: redrob-mysql
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redrob-mysql
  labels:
    app.kubernetes.io/name: redrob-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: redrob-mysql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: redrob-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          imagePullPolicy: IfNotPresent
          ports:
            - name: mysql
              containerPort: 3306
          envFrom:
            - secretRef:
                name: redrob-mysql
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command:
                - sh
                - -lc
                - mysqladmin ping -h 127.0.0.1 -uredrob -p"$MYSQL_PASSWORD"
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 24
      volumes:
        - name: data
          emptyDir: {}
