# 기여 안내

이 문서는 이 저장소의 작업 합의입니다. 브랜치를 어떻게 이름 짓는지, 머지 전에 무엇이 통과해야
하는지, 그리고 이 저장소가 독립 프로젝트가 아니라 **포크**이기 때문에 존재하는 두 가지 규칙을
다룹니다.

English: [CONTRIBUTING.md](./CONTRIBUTING.md)

## 포크이기 때문에 지켜야 하는 두 가지

다른 무엇보다 먼저 읽으십시오. 둘 다 관례가 아니라 CI가 강제합니다.

1. **`ee/` 아래에 무엇도 추가하지 않습니다.** 업스트림은 그 트리를 OpenWork Enterprise Edition
   License로 배포하고, 그 라이선스는 프로덕션 사용에 OpenWork 구독을 요구합니다. 이 포크는 MIT
   라이선스 내용만 담습니다.
2. **업스트림 저작권 표기를 교체하지 않습니다.** MIT는 모든 복제본에 그 표기가 유지될 것을
   요구합니다. 본인 이름을 추가하되, 그들의 것을 빼지 마십시오.

`pnpm check:upstream-boundary`가 둘 중 하나라도 어기면 실패합니다. 업스트림 변경을 가져오는
방법은 [docs/UPSTREAM.md](./docs/UPSTREAM.md)에 있습니다.

## 브랜치 흐름

gitflow입니다. `develop`이 통합하고, `main`은 릴리스된 상태입니다.

```
main    ──────●──────────────────────●──►   릴리스된 상태, 태그가 붙는 곳
             /                      /
develop ──●─●────●──────●──────────●────►   기본 브랜치, 모든 작업이 모임
              \        \
               ● fix/…  ● feat/…            수명이 짧고, 하나의 관심사만 담음
```

- **`develop`** 이 기본 브랜치이며 모든 PR의 베이스입니다. 브랜치를 자를 때도 PR을 열 때도 따로
  고르지 않아도 이쪽을 가리킵니다.
- **`main`** 은 릴리스된 상태입니다. `develop`을 머지해서 움직이고, 릴리스 태그는 여기서
  자릅니다. 태그 형식은 `v<major>.<minor>.<patch>` 입니다.
- **그래서 릴리스에는 승격 단계가 있습니다.** 릴리스 워크플로는 `origin/main` HEAD에 태그를
  붙입니다. `develop`을 `main`에 머지하기 전에 릴리스를 자르면 새 버전 번호를 달고 이전 상태가
  나갑니다. 그것을 막습니다. 새 릴리스 경로는 `develop`이 `main`보다 앞서 있으면 해당 커밋들을
  나열하며 실패하고, `allow_unpromoted: true` 가 이미 `main`에 올라간 핫픽스를 위한 예외입니다.
- **작업 브랜치** 는 `<type>/<짧은-슬러그>` 로 이름 짓습니다. 예를 들어
  `fix/composer-drop-zone`, `feat/reasoning-effort`, `chore/bump-electron`,
  `docs/upstream-sync`. 종류는 `feat`, `fix`, `chore`, `docs`, `test`, `refactor`, `perf`.
- **핫픽스** 는 `develop`이 아니라 `main`에서 자릅니다. 그래야 아직 릴리스되지 않은 작업을 함께
  끌고 가지 않습니다. `main`에 머지하고 릴리스한 뒤, **반드시 `main`을 `develop`으로 되돌려
  머지합니다.** 그러지 않으면 다음 릴리스에서 그 수정이 사라집니다. 이 단계가 빠지는 단계이고,
  형제 저장소인 엔진 쪽은 정확히 이 이유로 한 달 동안 기본 브랜치에서 lockfile이 깨진 채로
  있었습니다.
- **업스트림 동기화** 는 `sync/upstream-<날짜>` 에 올리고 스쿼시가 아니라 **머지 커밋** 으로
  머지합니다. 업스트림 머지를 스쿼시하면 머지 베이스가 사라지고, 다음 동기화가 이미 가져온 작업을
  다시 재생합니다.

리뷰를 요청하기 전에 `develop` 위로 리베이스하십시오. 브랜치는 좁게 유지합니다. 리뷰어가 그
브랜치가 무엇을 하는지 한 문장으로 말할 수 있어야 합니다.

**종류 목록과 되돌려 머지는 신뢰가 아니라
[`.github/workflows/gitflow.yml`](./.github/workflows/gitflow.yml) 이 강제합니다.** 헤드 브랜치가
위 종류 중 하나를 쓴 `<type>/<짧은-슬러그>` 가 아니면 PR을 실패시킵니다. 그래야 만든 도구의
이름을 딴 브랜치가 머지되는 대신 여기서 걸립니다. `develop` 과 `main` 자체는 통과합니다. 승격
브랜치와 되돌려 머지 브랜치는 종류를 따라 이름 짓지 않기 때문입니다. `main`에 푸시가 있을 때마다
같은 워크플로가 `main`에 `develop`에 없는 커밋이 남아 있으면 실패합니다. 핫픽스 항목이 말하는
규칙이자 실제로 빠지는 단계입니다. 두 잡 모두 테스트나 동작을 확인하지 않고, 위의 필수 체크
목록에도 들어 있지 않습니다. 그래서 머지를 막지 않고 보고만 합니다.

### 무엇이 보호되고 있고, 무엇은 보호할 수 없는가

`develop` 과 `main` 모두 보호됩니다. 모든 변경은 PR로 들어가고, 강제 푸시와 삭제는 막혀 있으며,
체크 두 개가 필수입니다.

| 항목 | 설정 |
|---|---|
| 필수 체크 | `i18n-audit`, `Syntax & dry-run publish` |
| 필수 리뷰 | 0 — 메인테이너 한 명이 자기 PR을 머지할 수 있음 |
| 최신 베이스 요구(strict) | 꺼짐 — 베이스가 낡아도 머지가 막히지 않음 |
| 관리자에게도 적용 | 꺼짐 |

**테스트 스위트는 필수 체크에 없고, 넣을 수도 없습니다.** `Redrob Cowork Tests` 는
`workflow_dispatch` 전용이라 PR에서 아예 보고하지 않습니다. 돌지 않는 체크를 필수로 걸면 모든 PR이
영원히 대기합니다. 위의 명령으로 로컬에서 돌리십시오. 트렁크의 기존 실패 두 개를 고친다면, 같은
변경에서 워크플로를 `pull_request` 에서 돌게 하고 필수 목록에 추가하십시오. 이 게이트를 의미 있게
만드는 것은 보호를 더 거는 것이 아니라 그것입니다.

## 커밋

Conventional 접두사, 명령형, 소문자 제목:

```
fix(composer): show the drop zone during dragover, not only on drop
```

값은 본문에 있습니다. 무엇이 잘못이었고 왜 이 수정이 올바른 형태인지 쓰십시오. diff가 이미
보여주는 것을 되풀이하지 마십시오. 증상이 나타난 곳과 원인이 있던 곳이 다르다면 그것을 쓰십시오.
다음 사람이 필요한 문장은 그것입니다.

## PR을 열기 전에

```bash
pnpm install
pnpm --filter @redrob/app test          # bun 테스트
pnpm --filter @redrob/app typecheck
node scripts/i18n-audit.mjs --ci        # 번역 키와 자리표시자
pnpm check:upstream-boundary            # 위의 두 가지 포크 규칙
pnpm check:outbound-access              # 선언되지 않은 네트워크 호스트 금지
```

**통과 여부는 종료 코드로 판단하십시오.** `pnpm test`는 `0 fail`을 찍으면서도 종료 코드 1로 끝날
수 있습니다. 파일이 로드에 실패하면 요약 옆에 `1 error` 한 줄로 나오고 그 파일의 테스트는 아예
세어지지 않습니다. 통과 개수만 보면 초록으로 보입니다.

사용자에게 보이는 문자열을 건드리면 규칙이 하나 더 붙습니다. **모든 문자열은 `t()`를 거치고**
`apps/app/src/i18n/locales/en.ts` 와 `ko.ts` 양쪽에 들어가야 합니다. 영어가 기준이고, 한국어 키가
없으면 화면이 깨지는 대신 영어로 대체됩니다. 한국어 값에 영어 단어가 남아 있으면 안 됩니다.
`apps/app/tests/supported-locales.test.ts`가 이를 강제하며, 정당한 기술 용어는 그 테스트의 허용
목록에 등록합니다.

## CI가 지금 확인하는 것과 하지 않는 것

| 워크플로 | 실행 시점 | 상태 |
|---|---|---|
| `i18n Audit` | PR, push | 필수 |
| `redrob-ui-mcp` | PR, push | 필수 |
| `Redrob Cowork Tests` | 수동 실행만 | **중지됨** |

테스트 워크플로는 의도적으로 중지되어 있고(`workflow_dispatch` 전용) 따라서 필수 체크가
**아닙니다.** 트렁크 자체에 기존 실패가 있습니다 — Ripgrep 타임아웃과 `util.flock` 오류 — 그래서
필수로 걸면 리뷰 대상과 무관한 이유로 모든 PR이 빨갛게 됩니다. 대신 위의 명령으로 로컬에서
돌리십시오. 그 두 실패를 고친다면, 같은 변경에서 워크플로를 `pull_request`에서 돌게 하고 필수로
지정하십시오.

## 리뷰

PR은 체크가 통과하고 메인테이너가 머지해야 합니다. 메인테이너 한 명이 자기 PR을 머지할 수
있으며, 현재 저장소에는 어느 쪽으로도 막는 설정이 없습니다.

머지 방식: 작업 브랜치는 **스쿼시**(하나의 관심사, `develop`에 하나의 커밋), 업스트림 동기화와
`develop`에서 `main`으로의 릴리스 승격은 **머지 커밋**. 이 둘은 리베이스 머지하지 마십시오. 머지를
스쿼시하거나 리베이스하면 다음 머지가 필요로 하는 관계가 사라집니다.

## 버그 신고와 기능 요청

`.github/ISSUE_TEMPLATE` 의 이슈 템플릿을 쓰십시오. 버그라면 쓸모 있는 신고는 플랫폼, 앱 버전,
화면에서 본 것을 적습니다. 실제 창을 찍은 스크린샷이 그것을 설명한 글보다 낫습니다.

## 보안

취약점은 공개 이슈로 열지 마십시오. [SECURITY.md](./SECURITY.md)가 있으면 그것을 따르고, 없으면
저장소 패키지 메타데이터의 주소로 메일을 보내십시오.

## 기여물의 라이선스

기여하면 본인의 작업이 트리의 나머지와 같은 MIT 라이선스로 배포되는 것에 동의하는 것입니다.
본인 기여물의 저작권은 본인이 유지합니다. [LICENSE](./LICENSE)의 표기는 기여자를 집합적으로
가리키며 무엇도 양도하지 않습니다.
