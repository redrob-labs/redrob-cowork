# 기여 가이드

이 문서는 이 저장소의 작업 규약입니다. 브랜치 이름, 머지 전에 초록불이어야 하는 것,
그리고 이 저장소가 **포크**이기 때문에 존재하는 두 가지 규칙을 다룹니다.

English: [CONTRIBUTING.md](./CONTRIBUTING.md)

## 포크 규칙 두 가지

다른 것보다 먼저 읽으십시오. 둘 다 관례가 아니라 CI가 강제합니다.

1. **`ee/` 아래에 아무것도 추가하지 않습니다.** 상류는 그 트리를 OpenWork
   Enterprise Edition 라이선스로 배포하며, 프로덕션 사용에 OpenWork 구독이
   필요합니다. 이 포크는 MIT 라이선스 콘텐츠만 싣습니다.
2. **상류 저작권 표기를 교체하지 않습니다.** MIT는 그 표기를 모든 복사본에
   유지하도록 요구합니다. 내 이름을 추가하되, 상류 것을 빼지 않습니다.

둘 중 하나라도 위반하면 `pnpm check:upstream-boundary`가 실패합니다. 상류 변경을
가져오는 방법은 [docs/UPSTREAM.md](./docs/UPSTREAM.md)에 있습니다.

## 브랜치 흐름

장기 브랜치 하나, 그 위에 단기 브랜치, 머지는 풀 리퀘스트로.

```
main ──●────●──────────●──────────────●──►   보호됨, 항상 릴리스 가능
        \        \            \
         ● fix/…  ● feat/…     ● chore/…     단기, 한 브랜치에 한 관심사
```

- **`main`**이 트렁크입니다. 보호돼 있어 직접 푸시·강제 푸시·삭제가 모두 막혀
  있고, 모든 변경은 풀 리퀘스트로 들어옵니다.
- **작업 브랜치**는 `<종류>/<짧은-설명>` 형식입니다. 예:
  `fix/composer-drop-zone`, `feat/reasoning-effort`, `chore/bump-electron`.
  종류: `feat`, `fix`, `chore`, `docs`, `test`, `refactor`, `perf`.
- **상류 동기화**는 `sync/upstream-<날짜>` 브랜치로 받고 **머지 커밋**으로
  넣습니다. 스쿼시하면 머지 베이스가 사라져서 다음 동기화가 이미 가져온 작업을
  다시 재생합니다.
- 릴리스 태그는 `main`에서 자릅니다. 형식은 `v<major>.<minor>.<patch>`.

리뷰 요청 전에 `main` 위로 리베이스하십시오. 브랜치는 좁게 유지합니다 — 리뷰어가
한 문장으로 "이 브랜치가 무엇을 한다"고 말할 수 있어야 합니다.

## 커밋

관용 접두사, 명령형, 소문자 제목:

```
fix(composer): show the drop zone during dragover, not only on drop
```

값은 본문에 있습니다. **무엇이 잘못돼 있었고 왜 이 수정이 맞는 모양인지**를 씁니다.
diff가 이미 보여주는 것을 반복하지 않습니다. 증상이 나타난 곳과 원인이 있던 곳이
다르면 그 사실을 적으십시오 — 다음 사람이 필요한 문장이 그것입니다.

커밋 메시지와 PR 본문은 다른 사람이 읽는 영구 기록이므로 영어로 씁니다.

## 풀 리퀘스트 전에 돌릴 것

```bash
pnpm install
pnpm --filter @redrob/app test          # bun 테스트
pnpm --filter @redrob/app typecheck
node scripts/i18n-audit.mjs --ci        # 번역 키와 자리표시자
pnpm check:upstream-boundary            # 포크 규칙 두 가지
pnpm check:outbound-access              # 선언되지 않은 네트워크 호스트 금지
```

사용자에게 보이는 문자열을 건드리면 규칙이 하나 더 붙습니다. **모든 문자열은
`t()`를 거치고**, `apps/app/src/i18n/locales/en.ts`와 `ko.ts` 양쪽에 들어갑니다.
영어가 원본이고, 한국어 키가 없으면 깨지는 대신 영어로 폴백합니다. 한국어 값에
영어 단어가 섞이면 `apps/app/tests/supported-locales.test.ts`가 실패합니다.
정당한 기술 토큰은 그 테스트의 허용 목록에 추가하십시오.

## 지금 CI가 검사하는 것과 하지 않는 것

| 워크플로 | 실행 시점 | 상태 |
|---|---|---|
| `i18n Audit` | 풀 리퀘스트, 푸시 | 필수 |
| `redrob-ui-mcp` | 풀 리퀘스트, 푸시 | 필수 |
| `Redrob Work Tests` | 수동 실행만 | **일시 중지** |

테스트 워크플로는 의도적으로 `workflow_dispatch`로만 잠겨 있고 필수 체크가
**아닙니다**. 트렁크 자체에 기존 실패가 있습니다 — Ripgrep 타임아웃과
`util.flock` 오류 — 그래서 필수로 걸면 리뷰 대상과 무관한 이유로 모든 풀 리퀘스트가
빨간불이 됩니다. 대신 위 명령으로 로컬에서 돌리십시오. 그 두 실패를 고치면, 같은
변경에서 워크플로를 `pull_request`에 다시 켜고 필수 체크로 올리십시오.

## 리뷰

체크가 초록불이고 메인테이너가 머지하면 됩니다. 메인테이너 한 명이 자기 PR을
머지해도 됩니다 — 보호 규칙은 실수로 직접 푸시하거나 강제 푸시하는 것을 막기 위한
것이고, 1인 변경에 형식을 만들려는 것이 아닙니다.

머지 방식: 작업 브랜치는 **스쿼시**(한 관심사, `main`에 한 커밋), 상류 동기화는
**머지 커밋**입니다. 상류 동기화를 rebase-merge 하지 마십시오.

## 버그 신고와 기능 요청

`.github/ISSUE_TEMPLATE`의 양식을 쓰십시오. 버그는 플랫폼, 앱 버전, 화면에서 본 것을
적어야 쓸모가 있습니다 — 실제 창 스크린샷이 그것에 대한 설명보다 낫습니다.

## 보안

취약점은 공개 이슈로 올리지 마십시오. [SECURITY.md](./SECURITY.md)가 있으면 그
절차를, 없으면 저장소 패키지 메타데이터의 주소로 메일 주십시오.

## 기여물의 라이선스

기여하면 그 작업이 트리의 나머지와 같은 MIT 조건으로 배포되는 데 동의하는 것입니다.
기여분의 저작권은 기여자 본인이 그대로 가지며, [LICENSE](./LICENSE)의 표기는
기여자를 뭉쳐서 명시할 뿐 권리를 양도받지 않습니다.
