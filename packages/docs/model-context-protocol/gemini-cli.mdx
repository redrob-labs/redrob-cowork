---
title: "Control Redrob Work from Gemini CLI"
sidebarTitle: "Gemini CLI"
description: "Add the local redrob-ui-mcp server to Gemini CLI so it can read Redrob Work's app context and run UI actions."
---

`redrob-ui-mcp` is a local stdio MCP server that exposes the running Redrob Work
desktop app: its current screen, its open sessions, and every UI action available
right now. Gemini CLI spawns it as a local MCP server, so there is no URL, no OAuth,
and no account. Read the [overview](/model-context-protocol/overview) for the tool
list and how the bridge works.

## Setup

1. Launch the Redrob Work desktop app. The local control bridge starts with it.

2. Add the server:

   ```sh
   gemini mcp add redrob-ui npx -y redrob-ui-mcp
   ```

3. Start Gemini CLI and list the servers:

   ```sh
   gemini
   ```

   ```text
   /mcp
   ```

## What you can ask

- "Use `ui_status` to check whether Redrob Work is reachable."
- "List my Redrob Work sessions, then open the one about the customer migration."
- "Type a summary request into the Redrob Work composer and send it."

## What's next

- Read the [overview](/model-context-protocol/overview) for the full tool and action
  reference.
- To give the Redrob Work agent its own access to a service, add that MCP server in
  the app: [Add an MCP server](/start-here/connect-your-stack/add-an-mcp-server).
