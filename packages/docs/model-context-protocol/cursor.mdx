---
title: "Control Redrob Cowork from Cursor"
sidebarTitle: "Cursor"
description: "Add the local redrob-ui-mcp server to Cursor so it can read Redrob Cowork's app context and run UI actions."
---

`redrob-ui-mcp` is a local stdio MCP server that exposes the running Redrob Cowork
desktop app: its current screen, its open sessions, and every UI action available
right now. Cursor spawns it as a local MCP server, so there is no URL, no OAuth, and
no account. Read the [overview](/model-context-protocol/overview) for the tool list
and how the bridge works.

## Setup

1. Launch the Redrob Cowork desktop app. The local control bridge starts with it.

2. Add the server to `~/.cursor/mcp.json` for every project, or `.cursor/mcp.json`
   for one project:

   ```json
   {
     "mcpServers": {
       "redrob-ui": {
         "command": "npx",
         "args": ["-y", "redrob-ui-mcp"]
       }
     }
   }
   ```

3. Open Cursor `Settings` > `MCP` and confirm `redrob-ui` is running.

<Note>
Cursor Web and Cursor Agents run away from your machine, so they cannot reach the
Redrob Cowork bridge on `127.0.0.1`. Use Cursor Desktop.
</Note>

## What you can ask

- "Use `ui_snapshot` to see what is on screen in Redrob Cowork."
- "List my Redrob Cowork sessions, then open the one about the customer migration."
- "Type a summary request into the Redrob Cowork composer and send it."

## What's next

- Read the [overview](/model-context-protocol/overview) for the full tool and action
  reference.
- To give the Redrob Cowork agent its own access to a service, add that MCP server in
  the app: [Add an MCP server](/start-here/connect-your-stack/add-an-mcp-server).
